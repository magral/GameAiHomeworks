Use arrow keys to control the grey (wolf) square. By default the first algorithm is pursue. You can click on red
and check off a different box to try a different algorithm. (But make sure only one algorithm is checked at a time, or else it'll just use the 
first algorithm checked).

I couldn't figure out how to appropriately draw a circle, but I did get the line on path following. Also there really isn't anything to
prevent character to go off the screen unfortunately. 

- Leah